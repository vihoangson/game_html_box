export const getters = {
  getCurrentPoints: (state) => state.currentPoints,
  getHighScore: (state) => state.highScore,
  getPointHistory: (state) => state.pointHistory,
  isGameActive: (state) => state.isGameActive
}

